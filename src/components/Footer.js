import React from "react";
import { Layout } from "antd";

const { Footer } = Layout;
const footComponet = () => {
  return (
    <Footer>
      <h4 className="foot">Map UI</h4>
    </Footer>
  );
};

export default footComponet;
